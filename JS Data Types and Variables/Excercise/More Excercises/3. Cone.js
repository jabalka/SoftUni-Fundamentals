function cone(radius, height){

    
// r = radius
// h = height
// s = slant height
// V = volume
// L = lateral surface area == πr√(r2 + h2)

// B = base surface area  ==  B = πr2B
// A = total surface area
// π = pi = 3.1415926535898
// √ = square root
//-----------------------
// Volume of a cone:
//    V = (1/3)πr2h
// ----------------------
// Total surface area of a cone:
//      A = L + B = πrs + πr2 = πr(s + r) = πr(r + √(r2 + h2))

let l = 

let volume = (1/3) * Math.PI * radius * 2 * height;

}